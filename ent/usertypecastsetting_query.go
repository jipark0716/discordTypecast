// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jipark0716/discordTypecast/ent/predicate"
	"github.com/jipark0716/discordTypecast/ent/usertypecastsetting"
)

// UserTypecastSettingQuery is the builder for querying UserTypecastSetting entities.
type UserTypecastSettingQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserTypecastSetting
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserTypecastSettingQuery builder.
func (utsq *UserTypecastSettingQuery) Where(ps ...predicate.UserTypecastSetting) *UserTypecastSettingQuery {
	utsq.predicates = append(utsq.predicates, ps...)
	return utsq
}

// Limit adds a limit step to the query.
func (utsq *UserTypecastSettingQuery) Limit(limit int) *UserTypecastSettingQuery {
	utsq.limit = &limit
	return utsq
}

// Offset adds an offset step to the query.
func (utsq *UserTypecastSettingQuery) Offset(offset int) *UserTypecastSettingQuery {
	utsq.offset = &offset
	return utsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utsq *UserTypecastSettingQuery) Unique(unique bool) *UserTypecastSettingQuery {
	utsq.unique = &unique
	return utsq
}

// Order adds an order step to the query.
func (utsq *UserTypecastSettingQuery) Order(o ...OrderFunc) *UserTypecastSettingQuery {
	utsq.order = append(utsq.order, o...)
	return utsq
}

// First returns the first UserTypecastSetting entity from the query.
// Returns a *NotFoundError when no UserTypecastSetting was found.
func (utsq *UserTypecastSettingQuery) First(ctx context.Context) (*UserTypecastSetting, error) {
	nodes, err := utsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usertypecastsetting.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) FirstX(ctx context.Context) *UserTypecastSetting {
	node, err := utsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserTypecastSetting ID from the query.
// Returns a *NotFoundError when no UserTypecastSetting ID was found.
func (utsq *UserTypecastSettingQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = utsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usertypecastsetting.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) FirstIDX(ctx context.Context) int {
	id, err := utsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserTypecastSetting entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserTypecastSetting entity is found.
// Returns a *NotFoundError when no UserTypecastSetting entities are found.
func (utsq *UserTypecastSettingQuery) Only(ctx context.Context) (*UserTypecastSetting, error) {
	nodes, err := utsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usertypecastsetting.Label}
	default:
		return nil, &NotSingularError{usertypecastsetting.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) OnlyX(ctx context.Context) *UserTypecastSetting {
	node, err := utsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserTypecastSetting ID in the query.
// Returns a *NotSingularError when more than one UserTypecastSetting ID is found.
// Returns a *NotFoundError when no entities are found.
func (utsq *UserTypecastSettingQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = utsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usertypecastsetting.Label}
	default:
		err = &NotSingularError{usertypecastsetting.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) OnlyIDX(ctx context.Context) int {
	id, err := utsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserTypecastSettings.
func (utsq *UserTypecastSettingQuery) All(ctx context.Context) ([]*UserTypecastSetting, error) {
	if err := utsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return utsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) AllX(ctx context.Context) []*UserTypecastSetting {
	nodes, err := utsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserTypecastSetting IDs.
func (utsq *UserTypecastSettingQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := utsq.Select(usertypecastsetting.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) IDsX(ctx context.Context) []int {
	ids, err := utsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (utsq *UserTypecastSettingQuery) Count(ctx context.Context) (int, error) {
	if err := utsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return utsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) CountX(ctx context.Context) int {
	count, err := utsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utsq *UserTypecastSettingQuery) Exist(ctx context.Context) (bool, error) {
	if err := utsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return utsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (utsq *UserTypecastSettingQuery) ExistX(ctx context.Context) bool {
	exist, err := utsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserTypecastSettingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utsq *UserTypecastSettingQuery) Clone() *UserTypecastSettingQuery {
	if utsq == nil {
		return nil
	}
	return &UserTypecastSettingQuery{
		config:     utsq.config,
		limit:      utsq.limit,
		offset:     utsq.offset,
		order:      append([]OrderFunc{}, utsq.order...),
		predicates: append([]predicate.UserTypecastSetting{}, utsq.predicates...),
		// clone intermediate query.
		sql:    utsq.sql.Clone(),
		path:   utsq.path,
		unique: utsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ActorID string `json:"actor_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserTypecastSetting.Query().
//		GroupBy(usertypecastsetting.FieldActorID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utsq *UserTypecastSettingQuery) GroupBy(field string, fields ...string) *UserTypecastSettingGroupBy {
	grbuild := &UserTypecastSettingGroupBy{config: utsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := utsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return utsq.sqlQuery(ctx), nil
	}
	grbuild.label = usertypecastsetting.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ActorID string `json:"actor_id,omitempty"`
//	}
//
//	client.UserTypecastSetting.Query().
//		Select(usertypecastsetting.FieldActorID).
//		Scan(ctx, &v)
func (utsq *UserTypecastSettingQuery) Select(fields ...string) *UserTypecastSettingSelect {
	utsq.fields = append(utsq.fields, fields...)
	selbuild := &UserTypecastSettingSelect{UserTypecastSettingQuery: utsq}
	selbuild.label = usertypecastsetting.Label
	selbuild.flds, selbuild.scan = &utsq.fields, selbuild.Scan
	return selbuild
}

func (utsq *UserTypecastSettingQuery) prepareQuery(ctx context.Context) error {
	for _, f := range utsq.fields {
		if !usertypecastsetting.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utsq.path != nil {
		prev, err := utsq.path(ctx)
		if err != nil {
			return err
		}
		utsq.sql = prev
	}
	return nil
}

func (utsq *UserTypecastSettingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserTypecastSetting, error) {
	var (
		nodes = []*UserTypecastSetting{}
		_spec = utsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserTypecastSetting).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserTypecastSetting{config: utsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (utsq *UserTypecastSettingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utsq.querySpec()
	_spec.Node.Columns = utsq.fields
	if len(utsq.fields) > 0 {
		_spec.Unique = utsq.unique != nil && *utsq.unique
	}
	return sqlgraph.CountNodes(ctx, utsq.driver, _spec)
}

func (utsq *UserTypecastSettingQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := utsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (utsq *UserTypecastSettingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   usertypecastsetting.Table,
			Columns: usertypecastsetting.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: usertypecastsetting.FieldID,
			},
		},
		From:   utsq.sql,
		Unique: true,
	}
	if unique := utsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := utsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usertypecastsetting.FieldID)
		for i := range fields {
			if fields[i] != usertypecastsetting.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := utsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utsq *UserTypecastSettingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utsq.driver.Dialect())
	t1 := builder.Table(usertypecastsetting.Table)
	columns := utsq.fields
	if len(columns) == 0 {
		columns = usertypecastsetting.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utsq.sql != nil {
		selector = utsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utsq.unique != nil && *utsq.unique {
		selector.Distinct()
	}
	for _, p := range utsq.predicates {
		p(selector)
	}
	for _, p := range utsq.order {
		p(selector)
	}
	if offset := utsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserTypecastSettingGroupBy is the group-by builder for UserTypecastSetting entities.
type UserTypecastSettingGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utsgb *UserTypecastSettingGroupBy) Aggregate(fns ...AggregateFunc) *UserTypecastSettingGroupBy {
	utsgb.fns = append(utsgb.fns, fns...)
	return utsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (utsgb *UserTypecastSettingGroupBy) Scan(ctx context.Context, v any) error {
	query, err := utsgb.path(ctx)
	if err != nil {
		return err
	}
	utsgb.sql = query
	return utsgb.sqlScan(ctx, v)
}

func (utsgb *UserTypecastSettingGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range utsgb.fields {
		if !usertypecastsetting.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := utsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (utsgb *UserTypecastSettingGroupBy) sqlQuery() *sql.Selector {
	selector := utsgb.sql.Select()
	aggregation := make([]string, 0, len(utsgb.fns))
	for _, fn := range utsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(utsgb.fields)+len(utsgb.fns))
		for _, f := range utsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(utsgb.fields...)...)
}

// UserTypecastSettingSelect is the builder for selecting fields of UserTypecastSetting entities.
type UserTypecastSettingSelect struct {
	*UserTypecastSettingQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (utss *UserTypecastSettingSelect) Scan(ctx context.Context, v any) error {
	if err := utss.prepareQuery(ctx); err != nil {
		return err
	}
	utss.sql = utss.UserTypecastSettingQuery.sqlQuery(ctx)
	return utss.sqlScan(ctx, v)
}

func (utss *UserTypecastSettingSelect) sqlScan(ctx context.Context, v any) error {
	rows := &sql.Rows{}
	query, args := utss.sql.Query()
	if err := utss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
